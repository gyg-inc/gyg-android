<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.capstonegyg.gyg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.Profile.ProfileActivity" />
        <activity android:name=".UI.Home.HomeActivity"></activity>
        <activity android:name=".UI.ViewGyg.ViewGygActivity"></activity>
        <activity android:name=".UI.PostGyg.PostGygActivity"></activity>
        <activity android:name=".UI.PaymentScreen.PaymentScreenActivity"></activity>
    </application>

</manifest>